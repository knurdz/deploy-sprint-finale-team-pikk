name: Leaky debug rehearsal

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Seeded token ${{ secrets.DEBUG_TOKEN }} should not be printed in real workflows"
